fastapi==0.128.0          # Async web framework; handles routing, request parsing, and auto docs
uvicorn==0.40.0           # ASGI server that actually runs the FastAPI app and listens for HTTP
httpx==0.28.1             # Async HTTP client; used for non-blocking calls to Groq streaming API
pydantic-settings==2.12.0 # Loads and validates env variables (.env file) into typed Python classes
structlog==25.5.0         # Structured JSON logging; machine-parseable, works well with log aggregators
sqlalchemy>=2.0           # ORM; maps Python classes to DB tables, handles queries without raw SQL
asyncpg>=0.29             # Async PostgreSQL driver; required by SQLAlchemy in async mode
alembic>=1.13             # DB migration tool for SQLAlchemy; versions and applies schema changes
pgvector>=0.3.0           # PostgreSQL vector type support; stores and queries embeddings for RAG
tiktoken>=0.7.0           # Tokenizer; counts tokens in text to stay within LLM context limits
pymupdf>=1.24.0           # PDF parser; extracts text and images page by page from PDF files
python-multipart>=0.0.9   # Enables file upload support (multipart/form-data) in FastAPI endpoints
pytest>=8.0               # Standard Python test framework; unit and integration tests
langsmith>=0.2.0          # LLM observability; traces prompts, responses, latency, and token usage