version: "3.9"

services:
  api:
    build: .
    container_name: groq_stream_api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 2
    depends_on:
      - db
      - mcp

  mcp:
    build: .
    container_name: groq_stream_mcp
    ports:
      - "8001:8001"
    env_file:
      - .env
    volumes:
      - .:/app
    command: uvicorn app.mcp_server.main:app --host 0.0.0.0 --port 8001

  db:
    image: postgres:15
    container_name: groq_stream_db
    environment:
      POSTGRES_USER: app
      POSTGRES_PASSWORD: app
      POSTGRES_DB: app
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
